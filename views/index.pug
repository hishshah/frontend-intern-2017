extends layout

block content
	center 
		<img src = "images/logo.png" style="margin-bottom: 80px; margin-top: 50px;" /><br>
		form(id="login" onsubmit="return validasi()" method="POST")
			<input type="text" name="username_or_email" placeholder="Email atau Username" />
			br
			<input type="password" name="password" placeholder="Password"/>
			br
			<input type="submit" name="submit" value="Log In"/>
			br
		div(class="lupaPass" onclick="popupBox()") Lupa password?
		br
		br
		button(onclick="popupBox()" class="button-login") <img src = "images/facebook-logo.png" width="12px" /> Log In dengan Facebook 
		br
		button(onclick="popupBox()" class="button-login") <img src = "images/twitter-logo.png" width="12px" /> Log In dengan Twitter
		br
		p(class="disclaimer") Prelo tidak akan post apapun tanpa persetujuan kamu
		hr
		p
			normal Belum punya ID Prelo? 
			b(onclick="popupBox()" class="b-login") Daftar sekarang